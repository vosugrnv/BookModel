import AppTabs from '@/components/app-tabs';
import React from 'react';

/** Nhóm tab bar (Khám phá / Hoạt động / Tài khoản). Các màn push khác nằm ở layout Stack gốc. */
export default function TabsGroupLayout() {
  return <AppTabs />;
}
